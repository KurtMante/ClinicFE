{
  "version": 2,
  "builds": [
    {
      "src": "clinic/package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "npm run build",
        "outputDirectory": "clinic/build"
      }
    }
  ],
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "clinic/build/index.html"
    }
  ]
}